{
  "extends": "./tsconfig.json",
  "//": "scripts/ queda afuera a propósito: un .ts fuera de src/ mueve el rootDir inferido y la salida pasa de dist/main.js a dist/src/main.js, que es lo que el deploy busca. Los scripts se corren con ts-node, que no usa este tsconfig.",
  "exclude": ["node_modules", "test", "dist", "scripts", "**/*spec.ts"]
}
